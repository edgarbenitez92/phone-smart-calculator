import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.phone.smartcalculator',
  appName: 'PhoneSmartCalculator',
  webDir: 'www',
};

export default config;
